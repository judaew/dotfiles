#!/usr/bin/env sh

if [ -z "$(which lf)" ]; then
    echo "Error: lf not found."
    return 1
fi

if [ "${1}" = "" ]; then
    echo 'Error: Enter the port you want to see'
    return 0
fi

lf "$(port dir "${1}")"
